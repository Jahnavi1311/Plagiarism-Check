# Plagiarism-Check
A simple Python program that detects similarity between multiple text documents using TF-IDF vectorization and cosine similarity. This project scans all .txt files in the current directory and displays similarity scores for every pair of documents.
